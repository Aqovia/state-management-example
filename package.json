{
  "name": "state-management-example",
  "version": "0.0.1",
  "license": "MIT",
  "scripts": {
    "postinstall": "npm install --prefix client && npm install --prefix server",
    "start": "concurrently -k -r \"npm start --prefix server\" \"npm start --prefix client\" "
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
